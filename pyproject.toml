[tool.poetry]
name = "e2e-project"
version = "0.1.0"
description = ""
authors = ["Mariia Varlamova"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
